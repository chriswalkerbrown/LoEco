{
  "providers": [
    {
      "type": "ttn",
      "name": "loeco_ttn",
      "enabled": true,
      "config": {
        "token": "${TTN_TOKEN}",
        "application_id": "test-field-lora-meteoa",
        "lookback": "168h"
      }
    },
    {
      "type": "ecowitt",
      "name": "ecowitt_home",
      "enabled": true,
      "config": {
        "application_key": "${WESTBROEKNO1ECOWITT_APP_KEY}",
        "api_key": "${WESTBROEKNO1ECOWITT_API_KEY}",
        "mac": "${WESTBROEKNO1ECOWITT_MAC}"
      }
    }
  ]
}
